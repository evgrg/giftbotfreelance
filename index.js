const db = require("./config.json");const count = require("./db.json");const users = require("./users.json");const fs = require("fs");const TelegramBot = require("node-telegram-bot-api");const { Keyboard } = require('telegram-keyboard');const bot = new TelegramBot(db.token, {  polling: true,});let keyboard1 = Keyboard.make([['СКАЧАТЬ ПОДАРОК'], ['АНКЕТА на РАЗБОР БИЗНЕСА']]);bot.on("message", async (msg) => {  const chat_id = msg.chat.id;  const command = msg.text.replace("/", "");  console.log(command);  console.log(chat_id);  let text = command.replace('send ', '');  if (command !== 'start' && command !== 'СКАЧАТЬ ПОДАРОК' && command !== 'АНКЕТА на РАЗБОР БИЗНЕСА') sendRequest(chat_id, text)  switch (command) {    case 'start':    return bot.sendMessage(        chat_id,        '👋 Привет! Для получения подарка "Инструкция: 10 ШАГОВ МАСШТАБИРОВАНИЯ БИЗНЕСА" нажмите на кнопку ниже:',        keyboard1.reply()      );    break;    case 'СКАЧАТЬ ПОДАРОК':      console.log(await checkSub(chat_id))      if (await checkSub(chat_id)) {        if(!users.includes(chat_id)) setTimerForRequest(chat_id);        bot.sendMessage(chat_id, '👇 Вот Ваш подарок');        bot.sendDocument(chat_id, db.pathToFile);        count.count = Number(count.count) + 1;        if (!users.includes(chat_id)) users.push(chat_id);        fs.writeFileSync('users.json', JSON.stringify(users));        fs.writeFileSync('db.json', JSON.stringify(count));        return sendInfo();      } else {        bot.sendMessage(chat_id, 'Для получения подарка, необходимо подписаться на канал: ' + db.channelName + '. После подписки вернитесь на эту страницу и нажмите на кнопку СКАЧАТЬ ПОДАРОК еще раз.')      }      break;    case 'АНКЕТА на РАЗБОР БИЗНЕСА':      bot.sendMessage(        chat_id,        'Для заполнения АНКЕТЫ на РАЗБОР в поле: Написать сообщение… введите ваши данные: \n \nВаше имя, Бизнес ниша, Оборот, Сайт, Телефон, E-Mail, Город'      )      break;  }});bot.on("callback_query", (query) => {  const {message: {chat, message_id} = {}} = query;  bot.answerCallbackQuery({    callback_query_id: query.id  });});async function checkSub(id, type) {    const data = await bot.getChatMember(db.channelName, id);    console.log(data);    if (data.status === "left" || data.status === "kicked") return false  else return true;}function sendRequest(chat_id, text) {  bot.sendMessage(chat_id, 'Ваша заявка успешно отправлена!');  bot.sendMessage(Number(db.adminId),'НОВАЯ ЗАЯВКА \n \n ' + text);}function setTimerForRequest(chat_id) {  console.log('start setTimeForRequest');  setTimeout(function () {    bot.sendMessage(      chat_id,      'Вы можете заполнить анкету на разбор, нажмите на кнопку внизу'    );  }, 300000);}function sendInfo() {  bot.sendMessage(Number(db.adminId),'Пользователь скачал файл. Общее количество скачиваний: ' + count.count);}console.log("Bot start!");